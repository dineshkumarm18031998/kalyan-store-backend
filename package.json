{
  "name": "kalyan-store-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm install && npm run prisma:generate",
    "start": "cd backend && npm start"
  }
}
